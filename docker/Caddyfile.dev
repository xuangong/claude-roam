# Development Caddyfile - proxy to Vite dev server
:80 {
    # API 请求代理到后端
    handle /api/* {
        reverse_proxy backend:8000
    }

    # 其他所有请求代理到 Vite dev server（包括 HMR websocket）
    handle {
        reverse_proxy frontend:5173
    }
}
